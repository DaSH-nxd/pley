import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
    colors: {
        primary: "#37447E",
        secondary: "#FFD166",
        turquoise: "#158582",
        teal: "#00C6C2",
        turtle: "#324B4A"
    },
});

export default theme;